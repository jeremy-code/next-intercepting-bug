const Default = () => {
  console.log(
    `The default component in app/default.tsx is being rendered with props`
  );

  return (
    <div>
      This is <code>default.tsx</code> and NOT the home page.
    </div>
  );
};

export default Default;
